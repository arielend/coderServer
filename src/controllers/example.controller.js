import { 
    createService,
    destroyManyService,
    destroyService,
    paginateService,
    readByEmailService,
    readLastByUserService,
    readLastService,
    readOneService,
    readService,
    updateService
} from '../services/carts.service.js'

class Controller {

    async create ( request, response, next ) {
        try {
            
        } catch (error) {
            return next(error)
        }
    }

    async destroyMany ( request, response, next ) {
        try {
            
        } catch (error) {
            return next(error)
        }
    }

    async destroy ( request, response, next ) {
        try {
            
        } catch (error) {
            return next(error)
        }
    }

    async paginate ( request, response, next ) {
        try {
            
        } catch (error) {
            return next(error)
        }
    }

    async readByEmail ( request, response, next ) {
        try {
            
        } catch (error) {
            return next(error)
        }
    }

    async readLastByUser ( request, response, next ) {
        try {
            
        } catch (error) {
            return next(error)
        }
    }

    async readLast ( request, response, next ) {
        try {
            
        } catch (error) {
            return next(error)
        }
    }
    
    async readOne ( request, response, next ) {
        try {
            
        } catch (error) {
            return next(error)
        }
    }

    async read ( request, response, next ) {
        try {
            
        } catch (error) {
            return next(error)
        }
    }

    async update ( request, response, next ) {
        try {
            
        } catch (error) {
            return next(error)
        }
    }
}

const controller = new Controller()
export const { 
    create,
    destroyMany,
    destroy,
    paginate,
    readByEmail,
    readLastByUser,
    readLast,
    readOne,
    read,
    update
} = controller